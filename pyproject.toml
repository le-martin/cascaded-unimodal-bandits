[project]
name = "cascaded-unimodal-bandits"
version = "0.1.0"
description = "Code for numerical simulations of cascaded unimodal Thompson sampling (CUTS) and its variants."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marimo>=0.17.0",
    "matplotlib>=3.10.7",
    "networkx>=3.5",
    "numpy>=2.3.4",
    "pandas>=2.3.3",
    "tqdm>=4.67.1",
]
